create table producto (
  codigo varchar(30),
  fecha_vencimiento date,
  descripcion varchar(50),
  cantidad integer
 );
create table sucursal (
  codigo varchar(30),
  nombre varchar(30),
  descripcion varchar(50)
 );
