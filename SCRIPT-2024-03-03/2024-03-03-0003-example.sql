create table repartidor (
  codigo varchar(30),
  fecha_vencimiento date,
  descripcion varchar(50),
  cantidad integer
 );
create table auxiliar (
  codigo varchar(30),
  nombre varchar(30),
  descripcion varchar(50)
 );
